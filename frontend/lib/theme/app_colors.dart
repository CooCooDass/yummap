import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFBC02D);      // Warm and bright Morning Yellow (Yellow 700)
  static const Color primaryLight = Color(0xFFFFFDE7); // Very soft pastel cream yellow (Yellow 50)

  static const Color secondary = Color(0xFFE67E22);    // Cozy orange accent color
  static const Color secondaryLight = Color(0xFFFDF2E9); // Soft apricot/cream background

  static const Color background = Colors.white;
  static const Color mapBackground = Color(0xFFF9F9F6); // Warm ivory/off-white background for surrounding elements

  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.grey;

  static const Color divider = Color(0xFFEEEEEE);
  static const Color error = Colors.redAccent;
}
